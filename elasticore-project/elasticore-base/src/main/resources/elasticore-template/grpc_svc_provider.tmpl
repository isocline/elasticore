// Initially generated by ElastiCORE; no longer managed—do not remove this line.
package ${packageName};


import io.grpc.stub.StreamObserver;
import io.elasticore.springboot3.mapper.Mapper;
import lombok.AllArgsConstructor;
import org.springframework.stereotype.Component;
import ${importList};


/**
 * gRPC Provider class for handling ${gRpcServiceName} service logic.
 *
 * Initially generated by ElastiCORE — a model-driven platform for
 * generating proto-based Spring Boot service scaffolding using DSL.
 *
 * This file is generated once and is not managed by ElastiCORE after creation.
 * You may freely modify the content as needed.
 *
 * To regenerate this class via ElastiCORE, delete the entire file before re-running the build.
 * ElastiCORE will then treat the class as missing and recreate it accordingly.
 */
@AllArgsConstructor
@Component
public class ${gRpcServiceName}Provider extends ${gRpcServiceName}Grpc.${gRpcServiceName}ImplBase {


    ${gRpcMethodList}
}