enumeration:
  MessageType:
    meta: enum @db(code) @json(code)  -- 메세지 타입
    fields:
      code: string(3) -- 타입코드
      name: string(50) -- 이름
    enum:
      SMS: SMS,단문문자
      LMS: LMS,장문문자
      CALL: CALL,전화발신
      KAKAO: KAKAO,카카오톡
      OUTBOUND_CALL: OBC,전화발신
      FAX: FAX,팩스

