dto:
  LoanCarDTO:
    meta: dto @template('LoanCar')
    fields:
      carSeq: int
      contrNm: string
      capital: string @disable
      picInfo: string @disable
      callInfos: string @disable
      processHistory: string @disable


  LoanCarSearchDTO:
    meta: dto @searchable(LoanCar)
    fields:
      test: string
